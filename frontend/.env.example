# Paradise JSON Sync - Environment Variables
# Copy this file to .env.local and configure as needed

# =============================================================================
# API Configuration / Configuración del API
# =============================================================================

# VITE_API_URL: Frontend API base URL
# - Development: Leave EMPTY to use Vite proxy (recommended)
# - Production: Set to your backend URL (e.g., https://api.yourdomain.com)
#
# VITE_API_URL: URL base del API para el frontend
# - Desarrollo: Dejar VACÍO para usar proxy de Vite (recomendado)
# - Producción: Configurar con URL del backend (ej: https://api.tudominio.com)
VITE_API_URL=

# VITE_API_TARGET: Backend target for Vite proxy (development only)
# Vite proxy redirects /api/* requests to this target
#
# VITE_API_TARGET: Target del backend para proxy de Vite (solo desarrollo)
# El proxy de Vite redirige peticiones /api/* a este target
VITE_API_TARGET=http://localhost:8000

# =============================================================================
# Optional Settings / Configuración Opcional
# =============================================================================

# API timeout in milliseconds (default: 30000)
# Timeout del API en milisegundos (por defecto: 30000)
# VITE_API_TIMEOUT=30000
