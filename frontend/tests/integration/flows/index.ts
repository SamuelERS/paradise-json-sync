/**
 * Integration Flow Tests Index / Índice de Tests de Flujo de Integración
 *
 * This file serves as documentation for the available integration tests.
 * Este archivo sirve como documentación para los tests de integración disponibles.
 *
 * Available test files / Archivos de test disponibles:
 * - uploadFlow.test.tsx: Tests for file upload flow / Tests para flujo de carga
 * - processFlow.test.tsx: Tests for file processing flow / Tests para flujo de procesamiento
 *
 * To run these tests / Para ejecutar estos tests:
 * ```bash
 * cd frontend
 * npm test -- tests/integration/flows/
 * ```
 *
 * These tests use MSW (Mock Service Worker) to mock API responses.
 * Estos tests usan MSW (Mock Service Worker) para simular respuestas de API.
 */

export {};
